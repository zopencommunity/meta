#ifndef ZOPEN_HTTPSPKG
  #define ZOPEN_HTTPSPKG 1
  #include <stdlib.h>

  int getfilenamefrompkg(const char* pkg, const char* pkgsfx, const char* tmppem, const char* tmppem2, char* buffer, size_t bufflen);
#endif
