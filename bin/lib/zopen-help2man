#!/bin/sh

#
# Routine to generate man pages from the --help and --version for the various tools
#
# We may choose to use --extendedhelp if the --help output is too long for folks
#

tooldirs="bin bin/lib"
rootdir=
